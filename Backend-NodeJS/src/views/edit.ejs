<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Phòng Khám</title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet"
            href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
        <script
            src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>
        <script
            src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
        <script
            src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>

        <style>
            /* Thêm một số kiểu CSS để mở rộng form */
            .form-container {
                max-width: 800px;
                width: 100%;
                padding: 20px;
                background-color: #f8f9fa;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

            .form-group input {
                font-size: 16px;
                padding: 15px;
            }

            .btn-block {
                padding: 12px;
                font-size: 18px;
            }
        </style>
    </head>
    <body>
        <!-- Sử dụng d-flex để căn giữa -->
        <div class="d-flex justify-content-center align-items-center vh-100">
            <form action="/edit" method="post"
                class="form-container border p-4 rounded shadow-sm bg-light">
                <div class="form-row">
                    <div class="col-12 mb-3 text-center">
                        <h4>Chỉnh Sửa</h4>
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputAddress">Họ Tên</label>
                    <input type="text" class="form-control" id="inputAddress"
                        name="fullname" value="<%= data.fullname %>">
                </div>
                <div class="form-group">
                    <label for="inputAddress2">Số Điện Thoại</label>
                    <input type="text" class="form-control" id="inputAddress2"
                        placeholder="Số điện thoại" name="phone"
                        value="<%= data.phone %>">
                </div>
                <div class="form-group">
                    <label for="inputAddress">Địa Chỉ</label>
                    <input type="text" class="form-control" id="inputAddress"
                        placeholder="1234 Main St" name="address"
                        value="<%= data.address %>">
                </div>

                <input name="id" value="<%= data.id %>" type="hidden">

                <button type="submit" class="btn btn-primary btn-block">Chỉnh
                    Sửa</button>
            </form>
        </div>
    </body>
</html>
